{% if recipient_role != 'lead' %}You are receiving this as the project's {{ 'administrator' if recipient_role == 'admin' else 'contact' }}.
The project lead is {{ project_lead }}{% if project_lead_email %} ({{ project_lead_email }}){% endif %}.

{% endif %}{% if operator_comment %}Note from NSF NCAR allocations staff:

{{ operator_comment }}

{% endif %}Take a look at the new SAM

SAM, the Systems Accounting Manager, was rebuilt from the ground up this year.
Much of what used to take a help-desk ticket you can now see, and do, yourself:

  - Manage your project yourself: add and remove members{% if recipient_role == 'lead' %} and set your
    project admin{% endif %}.
    {{ manage_url or 'https://sam.hpc.ucar.edu/' }}
{% if has_subtree %}
  - Move allocations between your sub-projects yourself, without asking for
    new quota — open your project above and use the allocations tab.
{% endif %}
  - See where your allocation is going: usage against each allocation, how
    your burn rate compares with the time remaining, and a breakdown by user.
    {{ (links or {}).accounts or 'https://sam.hpc.ucar.edu/user/accounts' }}

  - Job history: every job charged to your project, by user, queue and date,
    with wait-time, job-size and run-time distributions. My Jobs shows your
    own jobs across all of your projects.
    {{ (links or {}).jobs or 'https://sam.hpc.ucar.edu/user/jobs' }}

  - Disk usage in detail, for Campaign Storage: usage over time by user, your
    largest directories, and data nobody has touched in a long time.
    {{ (links or {}).data or 'https://sam.hpc.ucar.edu/user/data' }}

  - System status: Derecho, Casper and JupyterHub queues, node health and
    scheduled maintenance, no login required.
    {{ (links or {}).status or 'https://sam.hpc.ucar.edu/status/derecho' }}

  - Documentation: https://ncar-hpc-docs.readthedocs.io/
