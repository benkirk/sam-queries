*~
__pycache__/
.ipynb_checkpoints/

# NSF award-cache lock sidecar (created by nsf_awards.py resolver)
sql/queries/nsf_award_lookups.csv.lock
conda-env
conda-env-*/
containers/sam-sql-dev/dump/
collectors/logs/
logs/
*.egg-info/

# auth config:
.env
.cursor/

# skip linting reports in CI
megalinter-reports/

# pytest cruft
.coverage
.coverage.*
Vermilion_NSFNCAR-UCAR-UCP_BrandGuide.pdf
.playwright-mcp/
legacy_sam
data/

# Raw XRAS action payloads carry real names, emails and phone numbers. They are
# staged outside the working tree (~/xras_payloads_raw/) and reach the repo only
# after scripts/xras/scrub_payload.py. These patterns are the belt to that braces.
xras_payloads_raw/
*_payloads_raw/
