#-------------------------------------------------
# local container instance
#   "local SAM" is used especially for testing and
#   should always have read/write access
LOCAL_SAM_DB_USERNAME=root
LOCAL_SAM_DB_SERVER=127.0.0.1
LOCAL_SAM_DB_PASSWORD=root

#-------------------------------------------------
# Production SAM instance (read-only)
#   Used for accessing production data
#   Contact CISL staff for credentials
PROD_SAM_DB_USERNAME=your_prod_username
PROD_SAM_DB_SERVER=sam-sql.ucar.edu
PROD_SAM_DB_PASSWORD='your_prod_password'

#-------------------------------------------------
# Staging SAM instance (AWS RDS, requires UCAR VPN)
#   Get credentials: ./scripts/infra/db-creds-staging.sh
#   Switch with: ./scripts/setup/switch_to_staging_db.sh
# STAGING_SAM_DB_USERNAME=<from db-creds-staging.sh>
# STAGING_SAM_DB_SERVER=<from db-creds-staging.sh>
# STAGING_SAM_DB_PASSWORD=<from db-creds-staging.sh>

#-------------------------------------------------
# "proper SAM" is the SAM instance used to feed our dashboards etc...
#
# Pick ONE block below by uncommenting; all-commented is the default and
# routes the docker stack to the bundled `mysql` service (host `mysql`,
# root/root). Out-of-stack tools (sam-search, alembic) have no
# SAM_DB_SERVER until you uncomment one.
#
# IMPORTANT: what you uncomment also reaches the in-stack containers via
# compose `env_file:`. LOCAL therefore points the in-stack webapp at
# 127.0.0.1 — loopback inside the container, so webdev/webapp break; use
# it only for out-of-stack sam-search. PROD points both at
# sam-sql.ucar.edu, which is what you want for prod-data testing.

# Uncomment to use LOCAL (host loopback to docker mysql via 3306):
#SAM_DB_USERNAME=${LOCAL_SAM_DB_USERNAME}
#SAM_DB_SERVER=${LOCAL_SAM_DB_SERVER}
#SAM_DB_PASSWORD=${LOCAL_SAM_DB_PASSWORD}
#SAM_DB_REQUIRE_SSL=false

# Uncomment to use STAGING:
#SAM_DB_USERNAME=${STAGING_SAM_DB_USERNAME}
#SAM_DB_SERVER=${STAGING_SAM_DB_SERVER}
#SAM_DB_PASSWORD=${STAGING_SAM_DB_PASSWORD}
#SAM_DB_REQUIRE_SSL=false

# Uncomment to use PRODUCTION:
#SAM_DB_USERNAME=${PROD_SAM_DB_USERNAME}
#SAM_DB_SERVER=${PROD_SAM_DB_SERVER}
#SAM_DB_PASSWORD=${PROD_SAM_DB_PASSWORD}
#SAM_DB_REQUIRE_SSL=true

#-------------------------------------------------
# system_status database - used to populate the System Status
# Dashboard. STATUS_DB_DRIVER: "mysql" (default), "postgresql",
# or "postgres". Same pass-through rule as above — leaving the
# active lines commented lets compose.yaml route in-stack
# containers to the bundled `mysql` service.
STATUS_DB_DRIVER=mysql
# Uncomment for OUT-OF-STACK conda use against the docker mysql:
#STATUS_DB_USERNAME=root
#STATUS_DB_PASSWORD=root
#STATUS_DB_SERVER=127.0.0.1

#-------------------------------------------------
# Where our data collectors post their results
# STATUS_API_KEY is an API key (not a login password).  Generate a new pair with:
#   python scripts/gen_api_key.py --username collector
# The script prints two lines:
#   Collector .env  ->  STATUS_API_KEY=<key>       <- goes here
#   Webapp env var  ->  API_KEYS_COLLECTOR=<hash>  <- goes in compose.yaml / Helm values / SSM
STATUS_API_URL="http://localhost:5050"
STATUS_API_USER="collector"
STATUS_API_KEY="YOUR_GENERATED_API_KEY_HERE"
# API_KEYS_COLLECTOR="$2b$12$your_hash_here"      # webapp server-side hash (local dev)

# Legacy `api_credentials` DB table as a fallback source of Basic-Auth API keys
# (config API_KEYS above always wins). Lets existing legacy SAM API clients keep
# their credentials on the new API paths. Enabled rows are read live behind a
# TTL cache. NB: the dev clone TRUNCATEs api_credentials, so this only has data
# against the live/prod DB — insert a row locally to exercise it in dev.
# API_KEYS_DB_ENABLED=1
# API_KEYS_DB_TTL=60                               # seconds; 0 = no cache

#-------------------------------------------------
# Legacy-vs-new parity harness — utils/parity/check_legacy_apis.py
# SAM_NEW_API_* falls back to SAM_LEGACY_* when unset.
# SAM_XRAS_* is a SEPARATE account: /api/xras/* requires ROLE_XRAS, which the
# SAM_LEGACY_* account does not hold. One row in sam.api_credentials serves both
# the legacy Java app and the Python webapp, so the same pair works against
# either base URL.
# WARNING: ROLE_XRAS makes no method distinction — the same key authorizes
#    POST /api/xras/v1/actions. Treat it as a production WRITE credential.
# SAM_LEGACY_USER=
# SAM_LEGACY_PASS=
# SAM_LEGACY_URL=https://sam.ucar.edu
# SAM_NEW_API_USER=
# SAM_NEW_API_PASS=
# SAM_XRAS_USER=samuel
# SAM_XRAS_PASS=

#-------------------------------------------------
# XRAS Allocations API — SAM calling OUT to XRAS  (docs/xras/outgoing/)
#
# WARNING: NOT the same thing as SAM_XRAS_USER / SAM_XRAS_PASS above. Those are XRAS's
#    credential for calling SAM — a production WRITE credential in the inbound
#    direction. This key is SAM's credential for calling api.xras.org.
#
# The SAME key can create requests, merge one person into another, and modify
# roles. That is why the client is structurally GET-only: its one transport
# primitive is an internal _get, and a test pins that no write verb exists.
#
# Production reads the key from OpenBao (csg/xras-api-key, property `token`)
# via ExternalSecret. Locally, paste it below — and never commit a value.
#
# Fail-closed: with XRAS_OUTGOING_ENABLED=0 the dashboard card renders its
# unconfigured state, the xras_sweep task records a skip, and the CLI degrades.
# Nothing raises for lack of a key.
XRAS_OUTGOING_ENABLED=0
XRAS_API_KEY=
XRAS_API_BASE=https://api.xras.org
XRAS_ALLOCATIONS_PROCESS=NCAR
XRAS_API_USER=arcguest

#-------------------------------------------------
# JupyterHub Live Statistics API
# Used for real-time JupyterHub stats endpoints
JUPYTERHUB_API_URL=https://jupyterhub.hpc.ucar.edu
JUPYTERHUB_INSTANCE=stable
JUPYTERHUB_API_TOKEN=
JUPYTERHUB_CACHE_TTL=300

#-------------------------------------------------
# Email / notifications  (sam.notify — docs/plans/implemented/NOTIFICATION_FRAMEWORK.md)
#
# ndir.ucar.edu was probed from the cluster on 2026-08-09: it advertises
# STARTTLS and NO AUTH, and it relays for the whole 128.117.0.0/16 — which
# means any host on the VPN can mail ANY address on the internet through it.
# That is why NOTIFY_ENABLED defaults to 0 and why you should leave it there
# unless you specifically intend to send real mail from this checkout.
MAIL_SERVER=ndir.ucar.edu
MAIL_PORT=25
MAIL_USE_TLS=true
MAIL_TIMEOUT=10
# Inert against ndir (it advertises no AUTH); kept for a future relay.
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_DEFAULT_FROM=sam-admin@ucar.edu

# Master switch. FAIL-CLOSED: unset means nothing is delivered and every
# attempt is recorded 'suppressed'.
NOTIFY_ENABLED=0
# smtp | console (render to stdout, no socket) | null (record only)
NOTIFY_TRANSPORT=smtp
# When set, EVERY message is re-addressed here and recorded 'redirected'
# rather than 'sent'. The safe way to exercise the real send path.
NOTIFY_REDIRECT_TO=
# Envelope Bcc (comma-separated). Never emitted as a header.
NOTIFY_BCC=
# How long a 'queued' row blocks its own retry. An order of magnitude above
# MAIL_TIMEOUT: fresh means "in flight", stale means "we never learned".
NOTIFY_QUEUED_STALE_SECONDS=300

#-------------------------------------------------
# Flask Application Security
# Generate with: python -c 'import secrets; print(secrets.token_hex(32))'
FLASK_SECRET_KEY=your_generated_64_character_hex_string_here

#-------------------------------------------------
# Authentication for local development
#
# Default (recommended): AUTH_PROVIDER=stub plus the click-login page at
# /auth/login. Pick any of the DEV_QUICK_LOGIN_USERS buttons (benkirk
# ADMIN, mtrahan CSG, sureshm WNA_SCOPED_ADMIN, …) to land in different
# permission tiers — useful for exercising RBAC paths.
#
# Optional shortcut: set DISABLE_AUTH=1 plus DEV_AUTO_LOGIN_USER=<user>
# to auto-bind every request to a single user, skipping the login page
# entirely. Convenient when you're not testing auth/RBAC, but it hides
# permission boundaries — leave it off if you want the login page back.
# DISABLE_AUTH=1
# DEV_AUTO_LOGIN_USER=benkirk

#-------------------------------------------------
# OIDC Authentication (when AUTH_PROVIDER=oidc)
# Real OIDC is used in deployed environments only:
#   - Fargate staging:   https://sam-staging.csgsam.ucar.edu (env from AWS SSM)
#   - CIRRUS k8s:        https://sam.hpc.ucar.edu           (advertised name; env from OpenBao
#                        https://samuel.k8s.ucar.edu        via External Secrets. Both hosts serve
#                                                           the same deployment, in parallel.)
#
# Local OIDC against a real IdP (rare, only for protocol-level debugging):
#   1. Use a SEPARATE dev Entra app registration (NOT the staging/prod one) --
#      ask UCAR IT for a dev app with redirect URI http://localhost:5050/auth/oidc/callback.
#      Microsoft Entra allows http://localhost as a special exception for confidential apps.
#   2. Uncomment and fill the values below.
#   3. compose.yaml does NOT pass these envs through to the webdev container by default.
#      Add explicit env entries to compose.yaml's webdev service or `docker compose run -e`.
#   4. Use FLASK_CONFIG=development so SESSION_COOKIE_SECURE=False (HTTP loopback works).
#
# AUTH_PROVIDER=oidc
# OIDC_CLIENT_ID=your-DEV-entra-client-id
# OIDC_CLIENT_SECRET=your-DEV-entra-client-secret
# OIDC_ISSUER=https://login.microsoftonline.com/{tenant-id}/v2.0
# OIDC_REDIRECT_URI=http://localhost:5050/auth/oidc/callback
# OIDC_SCOPES=openid email profile
# OIDC_USERNAME_CLAIM=preferred_username
# FLASK_CONFIG=development

#-------------------------------------------------
# hpc-usage-queries plugin (per-job rows in the webapp)
#
# Separate package: `pip install -e '<path>/hpc-usage-queries[postgres]'`.
# It reads its OWN connection config (JOB_HISTORY_DB_BACKEND,
# JOB_HISTORY_PG_*) — see the plugin's .env.example. The vars below are
# SAM-side only: connection pooling and which machines are pre-warmed.
#
# Empty JOB_HISTORY_MACHINES disables the integration entirely.
# JOB_HISTORY_MACHINES=derecho,casper
# JOB_HISTORY_POOL_SIZE=5
# JOB_HISTORY_POOL_MAX_OVERFLOW=10
# JOB_HISTORY_POOL_RECYCLE=3600

#-------------------------------------------------
# fs-scans plugin (filesystem-scan analytics in the webapp)
#
# Ships in the SAME `hpc-usage-queries[postgres]` wheel as the plugin
# above; the importable package is `fs_scans`. Collection schemas are
# discovered at startup, so there is no machine list to set.
# FS_SCANS_ENABLED=0 skips plugin load entirely.
#
# Unlike job_history, FS_SCAN_PG_HOST is a directly-routable CNPG host —
# NOT host.docker.internal — so it passes through env_file unchanged.
#
# FS_SCAN_RESOURCE_DATABASES maps each scan resource to its CNPG database.
# Note the names differ: the Destor DB is `destor`, while `desc1` is only
# the Lustre mount /lustre/desc1. The plugin reaches the second database
# with a `database=` selector on the SAME host and credentials, so the role
# needs read access to both. Omit a resource to disable just that one.
# FS_SCANS_ENABLED=1
# FS_SCAN_DB_BACKEND=postgres
# FS_SCAN_PG_HOST=csg-postgres.k8s.ucar.edu
# FS_SCAN_PG_PORT=5432
# FS_SCAN_PG_USER=postgres
# FS_SCAN_PG_PASSWORD=
# FS_SCAN_PG_REQUIRE_SSL=false
# FS_SCAN_PG_DB=campaign
# FS_SCAN_RESOURCE_DATABASES=Campaign_Store:campaign,Destor:destor

#-------------------------------------------------
# MCP Server Configuration
# GitHub MCP Server - requires Personal Access Token with "repo" scope
#   Create token at: https://github.com/settings/tokens
#   Required scopes: repo
GITHUB_PERSONAL_ACCESS_TOKEN=your_github_token_here

# AWS MCP Server - AWS credentials (optional, can use AWS CLI default profile)
#   Can also use AWS CLI default profile instead of setting here
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_SESSION_TOKEN=your_aws_session_token_if_needed

# Local Variables:
# mode: sh
# End:
