# Configuration file for MegaLinter
#
# See all available variables at https://megalinter.io/latest/config-file/ and in
# linters documentation

# all, none, or list of linter keys
APPLY_FIXES: all

# If you use ENABLE variable, all other languages/formats/tooling-formats will
# be disabled by default
# ENABLE:

# If you use ENABLE_LINTERS variable, all other linters will be disabled by
# default
# ENABLE_LINTERS:

DISABLE_LINTERS:
  - DOCKERFILE_HADOLINT
  - REPOSITORY_CHECKOV
  - REPOSITORY_DEVSKIM

# DISABLE:
# - COPYPASTE # Uncomment to disable checks of excessive copy-pastes
# - SPELL # Uncomment to disable checks of spelling mistakes

SHOW_ELAPSED_TIME: true

# v9 introduced an "LLM Advisor" that calls an external LLM to explain linter
# findings. Pinned off explicitly rather than left to its default — a feature
# that makes outbound network calls from CI should be an opt-in decision.
LLM_ADVISOR_ENABLED: false

# Uncomment if you want MegaLinter to detect errors but not block CI to pass
DISABLE_ERRORS: true

# Vendored third-party assets (minified upstream dist files, pinned by
# sha384 in webapp/vendor_assets.py) — not ours to lint or spell-check.
FILTER_REGEX_EXCLUDE: (src/webapp/static/vendor/)

# linter-specific customizations
BASH_FILTER_REGEX_EXCLUDE: "(Dockerfile)"
BASH_SHFMT_ARGUMENTS: "--binary-next-line --case-indent --func-next-line --space-redirects --indent 4"
JSON_PRETTIER_FILTER_REGEX_EXCLUDE: '(\.cspell\.json)'
MARKDOWN_FILTER_REGEX_EXCLUDE: '(license\.md|docs/licenses)'
REPOSITORY_KICS_ARGUMENTS: --exclude-severities medium,low,info,trace
REPOSITORY_TRIVY_CONFIG_FILE: .trivy.yaml
SPELL_FILTER_REGEX_EXCLUDE: (\.json|\.github|\.mega-linter\.yaml)
