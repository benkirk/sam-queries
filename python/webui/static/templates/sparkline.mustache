{{!
    Sparkline Template
    Simple SVG line chart for usage trends (replaces Chart.js)

    Data structure:
    {
        id: "sparkline-1",
        data: [
            {date: "2024-11-01", value: 1234.56},
            {date: "2024-11-02", value: 2345.67},
            ...
        ],
        width: 400 (optional, defaults to 100%),
        height: 60 (optional, defaults to 60)
    }
}}
<div class="sparkline-container" id="{{id}}-container">
    <svg class="sparkline" viewBox="0 0 {{#width}}{{width}}{{/width}}{{^width}}400{{/width}} {{#height}}{{height}}{{/height}}{{^height}}60{{/height}}"
         preserveAspectRatio="none">
        {{! Area fill }}
        <polygon class="area" points="{{areaPoints}}" />

        {{! Line }}
        <polyline class="line" points="{{linePoints}}" />

        {{! Data points }}
        {{#points}}
        <circle class="point" cx="{{x}}" cy="{{y}}" r="3">
            <title>{{date}}: {{value}}</title>
        </circle>
        {{/points}}

        {{! Baseline }}
        <line class="baseline" x1="0" y1="{{baselineY}}" x2="{{#width}}{{width}}{{/width}}{{^width}}400{{/width}}" y2="{{baselineY}}" />
    </svg>
</div>
